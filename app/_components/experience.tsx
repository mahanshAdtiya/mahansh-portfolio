import { SectionHeading } from "./section-heading";

export function Experience() {
  return (
    <section
      id="experience"
      className="mx-auto max-w-page px-gutter pb-[clamp(56px,10vh,120px)]"
    >
      <SectionHeading
        num="02"
        title="Where the work happened"
        className="mb-[clamp(24px,5vh,56px)]"
      />
    </section>
  );
}
