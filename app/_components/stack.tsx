import { SectionHeading } from "./section-heading";

export function Stack() {
  return (
    <section
      id="stack"
      className="mx-auto max-w-page px-gutter py-[clamp(64px,14vh,150px)]"
    >
      <SectionHeading
        num="04"
        title="Stack"
        className="mb-[clamp(20px,4vh,36px)]"
      />
    </section>
  );
}
