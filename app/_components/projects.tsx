import { SectionHeading } from "./section-heading";

export function Projects() {
  return (
    <section
      id="projects"
      className="mx-auto max-w-page px-gutter pb-[clamp(64px,14vh,150px)]"
    >
      <SectionHeading
        num="05"
        title="Other projects"
        className="mb-[clamp(24px,5vh,48px)]"
      />
    </section>
  );
}
